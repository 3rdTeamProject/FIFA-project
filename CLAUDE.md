# 피파온라인 팀프로젝트 — 빅데이터활용 분석 모델 개발

## 프로젝트 개요
- 과제: 빅데이터활용 분석 모델 개발 (팀프로젝트, 팀원 3명)
- 소재: 피파온라인(EA SPORTS FC 온라인), 넥슨 오픈 API 사용
- 최종 주제: 유저 스쿼드 조합 승률 예측 + 플레이스타일 궁합 진단 대시보드
- 핵심 가치: 유저 닉네임을 입력하면 최근 매치 기록을 분석해 플레이스타일을 진단하고,
  현재 스쿼드가 그 스타일과 얼마나 궁합이 맞는지 점수로 보여준 뒤 해결방안을 제시한다.

## 왜 이 설계인가 (판단 기준)
- 이미 정해진 확률/공식으로 계산되는 값은 머신러닝이 아니다 (예: 강화 기대값 → 시뮬레이션일 뿐).
- 여러 변수의 복합 효과를 데이터로부터 찾아내야 하는 값만 머신러닝으로 취급한다 (예: 승률, 스타일 유형).
- 이 프로젝트는 지도학습(승률 예측)과 비지도학습(K-means 스타일 군집)을 모두 사용해 이 기준을 충족한다.

## 핵심 기능 (범위 확정됨)
1. 닉네임 입력 → "진단하기" 버튼
2. 최근 매치 기록(**최근 30경기**, 2026-09-07 확정)을 조회해 플레이스타일 진단 (PCA + K-means, 비지도학습).
   원래 "최대 100경기"였으나, 진단 1건당 매치 상세 조회가 경기 수만큼 필요해(닉네임→ouid 1회 +
   매치 목록 1회 + 매치 상세 N회) 100경기면 API 호출 ~102회·대기 20~50초가 걸려 하루 한도
   1,000건 기준 진단 가능 인원이 9~10명뿐이었다. 30경기로 줄이면 호출이 ~32회로 줄어 하루
   약 30명까지 진단 가능해지고, 넥슨 자체 서비스도 최근 전적 분석에 30경기 표본을 쓴다는
   점을 참고했다. K-means **학습**용 데이터(70경기 이상 유저만 정제해 수집, style/README.md
   참고)와는 별개 숫자다 — 학습은 표본이 많을수록 좋아 70+ 유지, 진단(추론)은 이미 학습된
   군집 중심에 가장 가까운 곳을 찾기만 하면 되므로 유저 본인 표본이 학습 표본만큼 클 필요가
   없다.
3. 가장 최근 경기 1건의 MATCH_PLAYER 11명 조합으로 "현재 스쿼드/포메이션"을 자동 구성 (별도 등록 불필요).
   ⚠️ 단순히 매치 목록의 첫 번째 경기를 쓰면 안 됨 — **몰수 경기(matchEndType ≠ 0)는
   `player[]` 배열이 비어있는 경우가 대부분**이라(2026-09-07 실측 확인), 목록에서
   `matchEndType == 0`인 가장 최근 경기를 찾아 그걸로 스쿼드를 구성해야 한다. (미해결 항목
   "몰수경기 필터링" 참고)
   ⚠️ "게임 내 지정 대표팀"을 가져오자는 대안은 검토 후 기각됨(2026-09-07) — Nexon 공식
   Open API·공식 사이트 어디에도 그런 조회 API가 없고, 있는 건 로그인 필요한 웹 프로필
   페이지뿐이라 크롤링+자동로그인은 계정 정지·비밀번호 저장 위험이 있어 채택하지 않음.
   대신 유저가 대시보드에서 스쿼드 슬롯을 다른 카드로 바꾸면 **API 재호출 없이** 그 자리에서
   즉시 재채점되도록 설계한다(`style/live_diagnosis.py`의 `score_squad()`가 순수 함수라
   가능 — user_style은 재사용, 바뀐 스쿼드만 다시 넣으면 됨).
4. 진단된 스타일과 현재 스쿼드를 대조해 포지션별/스쿼드 전체 궁합 점수 계산
   (포메이션 궁합 점수는 범위에서 제외됨 — 아래 "명시적으로 범위에서 제외한 것" 참고)
5. 궁합 점수가 낮은 부분에 대해 규칙 기반 문장으로 해결방안 제시
   (예: "당신은 스루패스 위주 스타일입니다. CM 포지션의 궁합이 낮은 편인데,
   패스 능력치가 높은 선수를 고려해보세요.")
   → 특정 카드를 콕 집어 추천하지 않는다. 방향성 제시까지만 한다.
6. (보조 기능, 핵심 아님) 승률 예측 모델 — "궁합 점수가 실제로 승률과 관련 있다"는 근거를 뒷받침하는 용도.
   앱 화면에서 크게 강조하지 않는다.

## 명시적으로 범위에서 제외한 것 (다시 꺼내지 말 것)
- 챗봇/OpenAI API 연동 UI → 대시보드형으로 확정, 재검토 불필요
- 유저가 직접 스쿼드를 검색·등록·저장하는 기능 → 매치 데이터에서 자동 구성하므로 불필요
- 특정 카드를 콕 집는 "추천" 기능 → 궁합 점수 진단 + 방향성 문장으로 대체
- 강화 단계별 비용 계산(확률 시뮬레이션이든 이적시장 시세 조회든) → 카드 추천을 안 하므로 완전히 불필요
- 게임플레이 영상 + 객체탐지(YOLO) 스타일 분석 → API 세부 스탯으로 충분, 필요시에만 백업 플랜으로 재검토
- 가상 공간에서 모델이 직접 게임을 플레이하게 하는 것(강화학습) → 프로젝트 범위를 크게 초과, 기각됨
- **포메이션 궁합 점수(formation_fit_score) / "이 스타일엔 4-3-3이 어울립니다" 식 포메이션 추천**
  (2026-09-12 결정) → 하지 않음. position_fit_score(카드 단위)/squad_fit_score(스쿼드 전체
  단위)까지만 계산한다. API가 포메이션별 spPosition 자리 조합표를 제공하지 않아 직접
  수작업으로 만들어야 했던 점(아래 미해결 항목이었던 "포메이션별 spPosition 자리 조합표
  미작성"), 그리고 "특정 카드를 콕 집어 추천하지 않는다"는 이미 정한 원칙과 같은 결로
  "특정 포메이션을 콕 집어 추천"하는 것도 범위를 넘어선다고 판단해 제외함.

## 데이터 수집 전략 (2트랙, 같은 API·같은 원본 저장 스키마 사용)
| 트랙 | 단위 | 목적 | 담당 |
|---|---|---|---|
| 승률학습용 | 1유저당 1경기, 스노우볼 방식으로 최대한 넓게 | WIN_PREDICTION_MODEL_INPUT 학습 데이터 | 데이터/API 담당(본인) |
| 스타일진단용 | 1유저당 최대 100경기, 깊게 | USER_STYLE_PROFILE 학습 데이터 | 모델 담당 |

- 두 트랙은 서로 다른 "수집 로직"일 뿐, 저장 스키마는 하나로 통일한다 (원본 통째로 저장 후 가공 단계에서 나눠 씀).
- 스타일진단용으로 모은 경기도 승률 예측 학습에 자유롭게 재사용 가능 (반대 방향은 불가 — 1경기짜리는 유저 평균을 못 냄).
- 유저가 두 트랙에 겹쳐 등장해도 전혀 문제없음. 오히려 효율적.

### 스노우볼 수집 방식
매치 상세 조회 응답의 `matchInfo` 배열에는 상대방 ouid·nickname도 함께 들어있다.
별도의 랭킹 API 없이, 팀원 닉네임을 시드로 상대방 닉네임을 계속 확장해나가며 수집한다.
(랭커 정보 조회 API의 `RankerStats`는 선수별 평균 통계만 제공하고 닉네임/순위 목록은 주지 않음 — 확인됨)

## 저장 방침 (통째 저장, 필터링은 가공 단계에서)
- API 응답은 **가공 없이 통째로** matches.jsonl에 저장한다. jsonl 용량 자체가 무시할 수준이라
  저장 시점에 필드를 미리 지울 이유가 없고, 이미 API 호출 비용을 들여 받은 데이터를
  저장 단계에서 버리면 나중에 다른 필드가 필요해졌을 때 재수집(API 호출 재낭비)이 필요해진다.
- `player[].status`, `shootDetail` 등 현재 안 쓰는 필드도 저장 시점에는 제거하지 않는다.
  "어느 필드를 쓸지 고르는 작업"은 CSV/학습 데이터로 가공하는 전처리 스크립트의 책임이며,
  거기서 필요한 필드(`player[].spId`, `spPosition`, `spGrade` 등)만 뽑아 쓴다.
- 중복 방지: `seen_ouid`, `seen_match_id`를 저장 직전에 체크해 API 호출과 중복 저장을 동시에 방지.

## Feature 선정 원칙 (승률 예측 / 스타일 진단 공통 상위 원칙)
1. **데이터 누수(leakage) 금지**: 승패와 사실상 동일한 정보(슛수, 골수, 태클 성공률, 경기 평점 등)는
   승률 예측 feature에서 절대 사용하지 않는다. "시도 방식"은 괜찮지만 "결과(골 여부)"는 안 된다.
2. **표본이 충분히 자주 발생하는가**: 페널티킥처럼 한 경기에 0~1번밖에 안 나오는 이벤트는
   비율로 만들면 노이즈가 커지므로 스타일 feature에서 제외한다.
3. **유저의 선택인가, 상황이 만드는 것인가**: 프리킥/페널티킥처럼 상대 반칙에 좌우되는 값은
   유저 스타일과 무관하므로 제외한다. 헤딩 슛 비중처럼 크로스 선호 등 실제 선택의 결과인 것은 포함한다.
4. **규칙 기반으로 미리 유형(개수/경계)을 정하지 않는다**: "3×2=6개 유형" 식으로 사람이 미리
   카테고리를 확정하면 비지도학습 취지에 어긋난다. K-means로 k=2~6을 다 시도해보고
   실루엣 점수/Elbow Method로 사후에 최적 k를 고른다. 그룹 이름도 군집 결과가 나온 뒤
   각 그룹의 feature 평균값을 보고 사후에 붙인다.

## 미해결 항목 (구현 전에 확정 필요)
- [x] **오버롤 필드 없음 → avg_stat_score로 대체 확정.** 선수 스탯 파일에 오버롤이 없어,
      세부 스탯(짧은패스/긴패스/드리블/볼컨트롤)의 단순 평균으로 대체하기로 결정.
      포지션별 가중평균은 가중치 근거가 약해 채택하지 않음.
- [ ] 위 파일 수치가 강화단계(spGrade) 반영값인지 기본값인지 불분명 — 확인 필요, 불분명하면 한계로 명시.
- [ ] `team_color`(팀컬러/케미) 필드가 실제 API 응답에 존재하는지 미확인 — 있으면 승률 예측 feature로 재검토.
- [x] matchtype 실제 코드값 확정 완료 — `/metadata/matchtype` API는 파라미터 오류로 응답하지 않아
      Key 불필요 정적 파일(`https://open.api.nexon.com/static/fconline/meta/matchtype.json`)로 확인.
      "1대1 공식경기"에 해당하는 코드는 **50 = 공식경기** (40 "클래식 1on1"은 이름만 비슷한 별개 구모드라 제외).
- [ ] API 키 rate limit(하루 1,000건)이 애플리케이션 단위인지 계정 단위인지 미확인
      (게임당 애플리케이션 3개, 애플리케이션당 키 2개까지 등록 가능 — 실측 필요)
- [x] 몰수경기(matchEndType ≠ 0) 필터링 여부 — **학습 데이터뿐 아니라 실시간 "현재 스쿼드"
      자동구성에도 반드시 제외해야 함이 실측으로 확인됨** (2026-09-07, 실제 닉네임으로 라이브
      진단 테스트 중 발견). 몰수 경기는 `matchInfo[].player[]` 배열이 통째로 비어있는 경우가
      대부분이라(정상종료 경기만 선수별 데이터가 채워짐), "가장 최근 경기 1건으로 스쿼드
      자동구성"(핵심 기능 3번)을 그대로 하면 그 경기가 몰수전일 때 스쿼드 구성 자체가
      실패한다. **대시보드 구현 시 반드시 반영**: 최근 매치 목록에서 `matchEndType == 0`인
      가장 최근 경기를 찾아 그걸로 스쿼드를 구성해야 하며(단순히 목록의 첫 번째를 쓰면 안 됨),
      스타일 진단용 비율 계산 쪽은 이미 style/preprocess_style.py에서 이 필터링을 하고 있었음.
- [ ] 스타일 진단용 유저의 최소 표본 경기 수 하한선 미확정 (예: 최소 10경기 이상만 채택 등)

## 성공 기준 (승률 예측 모델, 보조 기능)
- 완벽한 예측(60~70%)은 목표로 삼지 않는다 — 실력(손가락) 차이는 데이터로 측정 불가능해 완전 제거 불가.
- 정확도 55% 내외로 우연(50%)보다 유의미하게 나으면 성공으로 판단한다.
- 유저 실력 편차는 티어(division)를 feature로 포함해 부분적으로만 통제한다.

## ERD 핵심 엔티티 (자세한 것은 .claude/rules/data-schema.md 참고)
USER, MATCH, MATCH_PLAYER, PLAYER_CARD, USER_STYLE_PROFILE, WIN_PREDICTION_MODEL_INPUT
— MY_SQUAD/MY_SQUAD_SLOT은 제거됨 (최근 경기에서 자동 구성하므로 불필요).

## 팀 역할 분담
- 데이터/API (본인): 매치 API 검증, 승률학습용 스노우볼 수집 스크립트, matchtype/rate limit 등 API 세부사항 확인
- 모델: 스타일진단용 100경기 수집, PCA+K-means 스타일 진단, 승률 예측 모델(로지스틱회귀→랜덤포레스트) 학습
- 대시보드: Streamlit 구현, 해결방안 문장 템플릿 설계 (디자인 요소가 있어 본인 외 팀원이 담당)

## 발표 시 유의사항
- "머신러닝 vs 계산/통계" 차이를 초반에 짧은 비유로 설명 (확률을 이미 아는 경우 vs 데이터로 추정해야 하는 경우).
- 결론은 시각화 중심, 모델 원리 설명은 간단히.
- 손가락 문제·표본 편향 등 한계점을 스스로 인지하고 어떻게 완화했는지 명시하면 평가에 긍정적.
- 설계가 여러 번 바뀐 과정 자체를 "현실적 제약을 확인하며 범위를 조정해온 사례"로 보여줄 수 있음.
